# Generic vendor template

Clone `../descriptors/generic-opc-classic-template.json`, replace placeholder identity and arguments, and remove unsupported capabilities. Never add product binaries, secrets, customer paths, or setup commands.
