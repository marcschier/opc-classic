# Generic vendor template

Clone `../descriptors/generic-opc-classic-template.json`, replace placeholder
identity and arguments, and remove unsupported capabilities. Keep artifact
requirements relative to an allow-listed operator root. Never add product
binaries, license files, secrets, customer paths, proprietary captures, setup
commands, or executable arguments.
