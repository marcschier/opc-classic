<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Cross-spec primitives (OpcVariant, OpcQuality, OpcUrl, FILETIME helpers, NDR codec primitives) for the OPC Classic .NET stack.</Description>
    <RootNamespace>OpcClassic</RootNamespace>
    <AssemblyName>OpcClassic.Core</AssemblyName>
  </PropertyGroup>

  <!-- OpcClassic.Generators is wired in centrally via src/Directory.Build.props. -->

</Project>
